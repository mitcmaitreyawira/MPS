# Production Docker environment variables for MPS
NODE_ENV=production
PORT=3002
# Database
MONGODB_URI=
# Redis
REDIS_URL=redis://redis:6379
# JWT secrets are injected via CI/CD secrets
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
# CORS origins for production (comma-separated)
CORS_ORIGIN=https://app.example.com